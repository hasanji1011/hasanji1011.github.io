import { HighMapsDirective } from './high-maps.directive';

describe('HighMapsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighMapsDirective();
    expect(directive).toBeTruthy();
  });
});
