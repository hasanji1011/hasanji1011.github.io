import { WorldmapDirective } from './worldmap.directive';

describe('WorldmapDirective', () => {
  it('should create an instance', () => {
    const directive = new WorldmapDirective();
    expect(directive).toBeTruthy();
  });
});
