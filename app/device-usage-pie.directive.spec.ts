import { DeviceUsagePieDirective } from './device-usage-pie.directive';

describe('DeviceUsagePieDirective', () => {
  it('should create an instance', () => {
    const directive = new DeviceUsagePieDirective();
    expect(directive).toBeTruthy();
  });
});
