import { HighChartsDirective } from './high-charts.directive';

describe('HighChartsDirective', () => {
  it('should create an instance', () => {
    const directive = new HighChartsDirective();
    expect(directive).toBeTruthy();
  });
});
