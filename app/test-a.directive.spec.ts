import { TestADirective } from './test-a.directive';

describe('TestADirective', () => {
  it('should create an instance', () => {
    const directive = new TestADirective();
    expect(directive).toBeTruthy();
  });
});
